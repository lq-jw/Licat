using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class System_Setting : MonoBehaviour
{

    // private float f_volume_mainVolume = 0.5, f_volume_BGM = 0.5, f_volume_SE = 0.5;
    // private String list_screenMode[1] = { "fullScreen", "windows" },
    //             list_language[1] = { "zh-tw", "en" },
    //             set_screen = "fullScreen", set_language = "zh-tw";
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

// public float GetMainVolume(){

// }
}
